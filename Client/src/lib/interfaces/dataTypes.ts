export interface Championship {
  name: string,
  logoUrl: string,
  region: string
};

export interface Player {
  name: string,
  photoUrl: string
};

export interface Team {
  name: string,
  logoUrl: string,
  acronym: string
};